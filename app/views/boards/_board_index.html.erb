<div id="<%= dom_id board %>">
<br>
    <div class="song-image song-image-size">
      <%= image_tag board.song.image %>
    </div>

    <div class="song-infomation">
      <%= board.song.name %> / <%= board.song.artist.name %>
    </div>
    <br>
</div>
