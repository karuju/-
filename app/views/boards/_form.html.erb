<%= form_with(model: board, data: { turbo: false }) do |form| %>
  <% if board.errors.any? %>
    <div style="color: red">
      <ul>
        <% board.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  <%= form.label :"タイトル", style: "display: block" %>
  <%= form.text_field :title, style: "width: 100%;" %>
  </div>

  <div>
    <%= form.label :"コメント", style: "display: block" %>
    <%= form.text_area :body, rows: "20", style: "width: 100%;"%>
  </div>
  <% if @song.present? %>
    <%= form.hidden_field :song_id, value: @song.id %> 
  <% end %>

  <div>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
