  <div class="to-content-page">
    <h4><i class="fa-solid fa-book-open"></i><%= @comic.title %> /
    <%= @comic.author %> / 
    <%= I18n.t("activerecord.enum.comic.category.#{@comic.category}")%></h4>
  </div>
