<div id="<%= dom_id answer %>">
<%= image_tag(answer.user.image.present? ? answer.user.image_url : 'user_default.jpg') %>
<%= answer.user.name %>
<%= answer.body %>

<p>
<% if current_user %>
  <% @likeable = answer %>
  <%= render 'likes/like_button' %>
<% end %>
</p>
</div>
